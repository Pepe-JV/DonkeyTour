<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}ZenPaw Tech - Blog{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🐾</text></svg>">

        <!-- Tailwind CSS -->
        <script src="https://cdn.tailwindcss.com"></script>

        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="bg-gray-50">
        <div class="flex flex-col min-h-screen">
            <!-- Header -->
            <header class="bg-blue-600 text-white px-8 py-4 shadow-md">
                <h1 class="text-2xl font-bold m-0">🐾 ZenPaw Tech</h1>
                <p class="text-sm opacity-90 mt-1">Blog de tecnología y desarrollo</p>
            </header>

            <!-- Content wrapper con sidebar y main -->
            <div class="flex flex-1 flex-col md:flex-row">
                <!-- Sidebar -->
                <aside class="w-full md:w-64 bg-white border-b md:border-b-0 md:border-r border-gray-200 p-8 md:sticky md:top-0 md:h-screen md:overflow-y-auto">
                    <div class="mb-8">
                        <h3 class="text-lg font-bold text-blue-600 mt-0 mb-4">Navegación</h3>
                        <nav>
                            <ul class="list-none p-0 m-0">
                                <li class="mb-2">
                                    <a href="{{ path('app_blog') }}" class="block px-4 py-2 rounded transition-colors duration-200 no-underline {% if app.request.attributes.get('_route') == 'app_blog' %}bg-blue-600 text-white{% else %}text-gray-700 hover:bg-gray-100{% endif %}">
                                        🏠 Inicio
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="{{ path('app_category_index') }}" class="block px-4 py-2 rounded transition-colors duration-200 no-underline {% if app.request.attributes.get('_route') starts with 'app_category' %}bg-blue-600 text-white{% else %}text-gray-700 hover:bg-gray-100{% endif %}">
                                        📚 Categorías
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="{{ path('app_about') }}" class="block px-4 py-2 rounded transition-colors duration-200 no-underline {% if app.request.attributes.get('_route') == 'app_about' %}bg-blue-600 text-white{% else %}text-gray-700 hover:bg-gray-100{% endif %}">
                                        ℹ️ Acerca de
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="block px-4 py-2 rounded transition-colors duration-200 no-underline text-gray-400 cursor-not-allowed">
                                        🏷️ Etiquetas
                                    </a>
                                </li>
                            </ul>
                        </nav>
                    </div>

                    <div class="mb-8">
                        <h4 class="text-xs uppercase tracking-wide text-gray-500 mb-2">Acerca de</h4>
                        <p class="text-sm text-gray-600">
                            Blog dedicado a compartir conocimientos sobre desarrollo web, programación y tecnología.
                        </p>
                    </div>

                    <div class="mb-8">
                        <h4 class="text-xs uppercase tracking-wide text-gray-500 mb-2">Enlaces</h4>
                        <nav>
                            <ul class="list-none p-0 m-0">
                                <li class="mb-2">
                                    <a href="#" class="block px-4 py-2 rounded transition-colors duration-200 no-underline text-gray-700 hover:bg-gray-100">
                                        GitHub
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="block px-4 py-2 rounded transition-colors duration-200 no-underline text-gray-700 hover:bg-gray-100">
                                        Twitter
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="block px-4 py-2 rounded transition-colors duration-200 no-underline text-gray-700 hover:bg-gray-100">
                                        LinkedIn
                                    </a>
                                </li>
                            </ul>
                        </nav>
                    </div>
                </aside>

                <!-- Main content -->
                <main class="flex-1 p-8 max-w-screen-xl">
                    {% block body %}{% endblock %}
                </main>
            </div>

            <!-- Footer -->
            <footer class="bg-white border-t border-gray-200 px-8 py-6 text-center text-gray-600">
                <p>&copy; {{ "now"|date("Y") }} ZenPaw Tech. Todos los derechos reservados.</p>
                <p class="mt-2">
                    <small>Desarrollado con ❤️ usando Symfony y Tailwind CSS</small>
                </p>
            </footer>
        </div>
    </body>
</html>
