{% extends 'base.html.twig' %}

{% block title %}Inicio - ZenPaw Tech{% endblock %}

{% block body %}
<div>
    <h1>Últimos Posts del Blog</h1>

    {% if posts is empty %}
        <article>
            <p>No hay posts publicados todavía.</p>
        </article>
    {% else %}
        {% for post in posts %}
            <article>
                <header>
                    <h2>
                        <a href="{{ path('app_blog_show', {'slug': post.slug}) }}">
                            {{ post.title }}
                        </a>
                    </h2>
                    <p>
                        <small>📅 Publicado el {{ post.publishedAt|date('d/m/Y H:i') }}</small>
                    </p>
                </header>

                {% if post.summary %}
                    <p>{{ post.summary }}</p>
                {% else %}
                    <p>{{ post.content|slice(0, 200) }}{% if post.content|length > 200 %}...{% endif %}</p>
                {% endif %}

                <footer>
                    <a href="{{ path('app_blog_show', {'slug': post.slug}) }}" role="button">
                        Leer más →
                    </a>
                </footer>
            </article>
        {% endfor %}
    {% endif %}
</div>
{% endblock %}
